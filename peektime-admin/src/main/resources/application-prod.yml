# 운영 환경 설정
# 환경변수 플레이스홀더 사용 (.env → Docker/배포 시스템 → 환경변수 주입)

gemini:
  api-key: ${GEMINI_API_KEY}

spring:
  datasource:
    url: ${DATASOURCE_URL}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
